# Bandit3.md
Bandit 3 tasks you with finding a hidden file, within a directory called "inhere".

We `cd` into the inhere/ directory then use `ls -la` to display a file called "...Hiding-From-You".

`cat ...Hiding-From-You` gives us the flag.
